import React from 'react';
import ProductList from '../component/prodictlist';

const HomePage = () => {
    return (
        <div>
           <ProductList/>
        </div>
    );
};

export default HomePage;
